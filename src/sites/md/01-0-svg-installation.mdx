import Chapter from "../../components/Chapter"
import Example from "../../components/Example"

<Chapter>

## Github Classrooms

<section>

Gehen Sie auf die Webseite, welche die Lehrperson mit Ihnen geteilt hat.
Sie sollten dann Zugriff erhalten auf Ihr eigenes Repository (Code-Bibliothen), welche Sie für das Erlernen von SVG verwenden werden.

Wechseln Sie dann zurück ins VSCode und drücken Sie `CTRL + SHIFT + P` und
tippen Sie `clone`. Wählen Sie dann `Git: Clone` aus, und klonen Sie das
folgende Repository zu Ihnen auf den Computer. (stellen Sie sicher, dass Sie den richtigen Link kopieren)

```text
https://github.com/gym-web-coding/[...].git
```

</section>

</Chapter>
